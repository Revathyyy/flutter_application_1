import 'package:flutter/material.dart';
import './button.dart';
List<Widget> buttonList=[
const Buttons(
  label:"C",
  textColor: Colors.blue,
),
const Buttons(
  label:"/",
  textColor: Colors.blue,
),
const Buttons(
  label:"X",
  textColor: Colors.blue,
),
const Buttons(
  label:"AC",
  textColor: Colors.blue,
),
const Buttons(
  label:"1",
),
const Buttons(
  label:"2",
),
const Buttons(
  label:"3",
),
const Buttons(
  label:"-",
  textColor: Colors.blue,
),
const Buttons(
  label:"4",
),
const Buttons(
  label:"5",
),
const Buttons(
  label:"6",
),
const Buttons(
  label:"+",
  textColor: Colors.blue,
),
const Buttons(
  label:"7",
),
const Buttons(
  label:"8",
),
const Buttons(
  label:"9",
),
// const Buttons(
//   label:"4",
//   textColor: Colors.blue,
// ),
const Buttons(
  label:"%",
  textColor: Colors.blue,
),
const Buttons(
  label:"0",
),
const Buttons(
  label:".",
  textColor: Colors.blue,
),
// const Buttons(
//   label:"4",
//   textColor: Colors.blue,
// ),

];